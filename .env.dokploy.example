# Environment Variables Template for Dokploy Deployment
#
# Copy this to Dokploy's environment configuration when deploying TranslateFlow.
# Generate secure values for all secrets before deploying to production.
#
# SECURITY NOTES:
# - Generate strong random values for all SECRET_KEY, passwords, and API keys
# - Never commit actual secrets to version control
# - Use Dokploy's encrypted environment variables feature
# - Rotate credentials periodically

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Option 1: Use Dokploy's managed PostgreSQL (RECOMMENDED)
# Dokploy will provide the connection string. Format:
# DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_URL=

# Option 2: Use individual database parameters (if not using DATABASE_URL)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=translateflow
DB_USER=translateflow
DB_PASSWORD=changeme  # ⚠️ CHANGE IN PRODUCTION
USE_SQLITE=false

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Secret key for JWT tokens and session encryption
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=changeme-in-production-please-generate-a-secure-key  # ⚠️ CHANGE IN PRODUCTION

# Application server configuration
HOST=0.0.0.0
PORT=8000

# Python settings
PYTHONUNBUFFERED=1
RUNNING_IN_DOCKER=true

# ============================================================================
# EMAIL CONFIGURATION
# ============================================================================

# Email provider: resend, sendgrid, or smtp
EMAIL_PROVIDER=

# Resend (https://resend.com)
RESEND_API_KEY=

# SendGrid (https://sendgrid.com)
SENDGRID_API_KEY=

# SMTP Configuration (for custom SMTP server)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true

# Email display settings
EMAIL_FROM=noreply@translateflow.yourdomain.com
EMAIL_FROM_NAME=TranslateFlow
EMAIL_REPLY_TO=support@yourdomain.com

# ============================================================================
# STRIPE PAYMENT CONFIGURATION
# ============================================================================

# Stripe API keys (https://stripe.com/docs/keys)
STRIPE_API_KEY=sk_test_...  # ⚠️ USE LIVE KEY IN PRODUCTION

# Stripe webhook secret (for handling payment events)
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs for subscription plans
# Get these from your Stripe Dashboard > Products
STRIPE_PRICE_FREE=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=

# ============================================================================
# OAUTH PROVIDERS (OPTIONAL)
# ============================================================================

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth redirect URI (must match your OAuth app configuration)
OAUTH_REDIRECT_URI=https://translateflow.yourdomain.com/auth/callback

# ============================================================================
# DOKPLOY-SPECIFIC SETTINGS
# ============================================================================

# Resource limits (adjust based on your Dokploy plan)
# These should match your Dokploy service configuration
# DOKPLOY_CPU_LIMIT=2
# DOKPLOY_MEMORY_LIMIT=2G

# ============================================================================
# PRODUCTION CHECKLIST
# ============================================================================

# Before deploying to production, ensure:
#
# [ ] All SECRET_KEY values are generated with strong randomness
# [ ] DATABASE_URL points to production database (not development)
# [ ] DB_PASSWORD is set to a strong password
# [ ] STRIPE_API_KEY uses live key (not test key)
# [ ] STRIPE_WEBHOOK_SECRET is configured for production
# [ ] EMAIL_PROVIDER is configured and tested
# [ ] OAUTH_REDIRECT_URI matches your production domain
# [ ] SSL/HTTPS is enabled in Dokploy
# [ ] Automatic backups are enabled
# [ ] Log aggregation is configured
# [ ] Monitoring/alerting is set up
