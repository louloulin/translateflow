# TranslateFlow Environment Configuration
# Copy this file to .env and update the values for your deployment

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL Connection Settings
# These are used by docker-compose to configure the database
DB_NAME=translateflow
DB_USER=translateflow
DB_PASSWORD=changeme-please-use-a-strong-password

# Optional: Override DATABASE_URL (automatically constructed in docker-compose)
# DATABASE_URL=postgresql://translateflow:changeme@postgres:5432/translateflow

# SQLite Fallback (not recommended for Docker deployments)
# USE_SQLITE=false
# SQLITE_PATH=ainiee.db

# =============================================================================
# Application Configuration
# =============================================================================

# Secret key for JWT signing and session management
# IMPORTANT: Generate a secure random key for production!
# You can generate one with: openssl rand -hex 32
SECRET_KEY=changeme-in-production-please-generate-a-secure-key

# Server Configuration
PORT=8000
HOST=0.0.0.0

# Docker-specific flag (automatically set in docker-compose)
# RUNNING_IN_DOCKER=true

# =============================================================================
# Email Configuration (Optional but Recommended)
# =============================================================================

# Choose your email provider: resend, sendgrid, or smtp
EMAIL_PROVIDER=

# Resend (https://resend.com)
# RESEND_API_KEY=re_your_api_key_here

# SendGrid (https://sendgrid.com)
# SENDGRID_API_KEY=SG.your_api_key_here

# SMTP Configuration
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_username
# SMTP_PASSWORD=your_password
# SMTP_USE_TLS=true

# Email Sender Information
EMAIL_FROM=noreply@translateflow.local
EMAIL_FROM_NAME=TranslateFlow
EMAIL_REPLY_TO=

# =============================================================================
# Stripe Payment Configuration (Optional)
# =============================================================================

# Get your API keys from: https://dashboard.stripe.com/apikeys
# STRIPE_API_KEY=sk_test_your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Price IDs for each subscription tier
# Create products and prices in Stripe Dashboard, then add the price IDs here
# STRIPE_PRICE_FREE=price_xxxxx
# STRIPE_PRICE_STARTER=price_xxxxx
# STRIPE_PRICE_PRO=price_xxxxx
# STRIPE_PRICE_ENTERPRISE=price_xxxxx

# =============================================================================
# OAuth Providers (Optional)
# =============================================================================

# GitHub OAuth
# Create an OAuth App at: https://github.com/settings/developers
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth
# Create credentials at: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# OAuth Redirect URI (update for your domain)
OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback

# =============================================================================
# Production Checklist
# =============================================================================
# Before deploying to production, make sure to:
# [ ] Change DB_PASSWORD to a strong, unique password
# [ ] Generate a secure SECRET_KEY (use: openssl rand -hex 32)
# [ ] Configure email provider for notifications
# [ ] Set up Stripe for billing (if using paid plans)
# [ ] Update OAUTH_REDIRECT_URI to your production domain
# [ ] Review and uncomment optional services as needed
# [ ] Set up SSL/TLS certificates
# [ ] Configure backup strategy for database
# [ ] Change default admin password after first login (admin/admin)
